# Production Environment Variables
# Copy this file to the server and adjust values as needed

# Environment Configuration
ENVIRONMENT=production
DEBUG=false

# Database Configuration (AWS RDS)
DB_HOST=job-platform-db.cwdec2aoci4i.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=jobplatform
DB_USER=dbadmin
# Note: Set DB_PASSWORD in docker-compose or environment

# Security Configuration
# Note: Set JWT_SECRET and SECRET_KEY in docker-compose or environment
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Configuration
APP_NAME=Job Platform
APP_VERSION=1.0.0

# CORS Configuration - Dynamic (set by deployment script)
# CORS_ORIGINS=http://DYNAMIC_IP:3000,http://DYNAMIC_IP:8000

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Email Configuration (Optional - configure based on your SMTP provider)
# EMAIL_SMTP_SERVER=
# EMAIL_SMTP_PORT=587
# EMAIL_USERNAME=
# EMAIL_PASSWORD=
# FROM_EMAIL=noreply@jobplatform.com
# EMAIL_USE_TLS=true
# EMAIL_USE_SSL=false

# AWS Configuration
AWS_REGION=us-east-1
# AWS_SECRET_NAME=job-platform/database